
@model List<CheckSiteMap.UI.Models.SiteViewModel>

@{
    ViewBag.Title = "AllRequests";
}

<h2>AllRequests</h2>
<table class="table table-dark">
    <thead>
        <tr>
            <th scope="col">Url</th>
            <th scope="col">Summary Time, sec</th>
            <th scope="col">Time request</th>
            <th scope="col">Check</th>
        </tr>
    </thead>
    @using (Html.BeginForm())
    {
        foreach (var site in Model)
        {
            <tbody>
                <tr>
                    <td>@string.Join(" ", site.Url)</td>
                    <td>@string.Join(" ", site.SummaryTime)</td>
                    <td><a href="/Home/CheckRequest/@site.Id">Check</a></td>
                </tr>
                <tr>
            </tbody>
        }
    }
</table>
